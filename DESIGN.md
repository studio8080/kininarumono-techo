# DESIGN.md — 気になるモノ手帖 デザイン規約

UIを触る前に読むこと。トークンの実体は `public/css/style.css` の `:root`。**このファイルとCSSがズレたら、CSSを正とした上でこのファイルを直す。**

---

## 1. 方向性

**ポップ、ただし規律を持って。**

紙色の上に「墨の輪郭 + ぼかさないハード影」を全パーツに通した**ステッカーの言語**で統一する。色は**カテゴリの識別だけ**に使い、面で暴れさせない。

参照したのは以下2サイト。ピクセルではなく原理を抽出している。

| | 抽出した原理 |
|---|---|
| ikiiki-being.com | 主色1色をほぼ全面に効かせる規律（実測で主色が957箇所）／ピル形状の徹底（`border-radius:9999px` が37箇所）／巨大な和文ディスプレイ／YakuHanJPによる約物最適化／紙色のベース |
| sou-group.jp | 温かい黒（#1A1311）を地にする／カードごとに色を配分／上部を大きく丸めたアーチ型メディア／黒ピル＋丸アローのCTA／縁取りタグ／フラットな装飾図形 |

---

## 2. 色

```css
--paper:#FBF7EF;  --paper-2:#FFFDF8;   /* 紙 */
--ink:#17130E;    --ink-2:#4B433A;     /* 墨・本文 */
--muted:#6E675C;  --line:#E4DACA;
```

カテゴリ色は**3つ1組**。用途を混ぜないこと。

| カテゴリ | `--c`（面） | `--c-deep`（文字） | `--c-soft`（淡面） |
|---|---|---|---|
| gadget ガジェット | `#0A57FF` | `#003EBA` | `#E4ECFF` |
| interior インテリア | `#10B6A8` | `#067A72` | `#DFF5F3` |
| kitchen 食器・キッチン | `#12B76A` | `#14713A` | `#DFF3E6` |
| beauty コスメ・ケア | `#FF3D9A` | `#C4166B` | `#FFE4F0` |
| daily 日用品 | `#8B5CF6` | `#5B2AA8` | `#EDE6FF` |
| goods 文具・雑貨 | `#FF4B3E` | `#C92A1B` | `#FFE8E5` |
| fashion ファッション | `#FF9E1B` | `#8F5200` | `#FFF0DC` |

アクセント: `--pink:#FF3D9A` / `--pink-deep:#C4166B` / `--yellow:#FFC61A`

### なぜ3つに分かれているか

**`--c` を文字色や文字の背景に使うと WCAG AA を割る。** 白文字×アンバーは約2:1、白文字×ティールは約2.5:1しかない。実際に一度そのバグを作り込んでいる。

- 面・図案・影・装飾 → `--c`
- **文字色**（白地の上）→ `--c-deep`
- **文字の背景**（白文字を乗せる、チップの選択状態など）→ `--c-deep`
- タグの淡い下地（`--c-deep` の文字を乗せる）→ `--c-soft`

現状の実測値は最低4.58（`.card__cat`）。**新しい配色を足したら必ず測る。**

---

## 3. タイポグラフィ

```css
--display:"YakuHanJP","Dela Gothic One","Zen Maru Gothic",sans-serif;  /* h1,h2,ワードマーク */
--jp:"YakuHanJP","Zen Maru Gothic",system-ui,sans-serif;               /* 本文・UI・h3 */
--latin:"Outfit",var(--jp);                                            /* 欧文・数字 */
```

- **現在のh1/h2は `--jp` の900で統一。** Dela Gothic One は読み味が強すぎるため主見出しから外した。戻す場合は400のみで使い、合成太字にしない。
- 字幅が広いので `clamp()` の上限は控えめ（ヒーロー `clamp(34px,7.4vw,92px)`）。書体を替えたら必ず再調整する。
- `body` に `font-feature-settings:"palt" 1`。YakuHanJP をスタックの先頭に置いて和文約物（「」、。）だけ半角に差し替えている。
- **句点を装飾要素の外に置かない。** 約物が半角化して宙に浮いた丸に見える。ヒーローでは `<span class="pop">アガる。</span>` のようにステッカー内に入れている。

---

## 4. 形とステッカー言語

```css
--r-card:26px;    /* カード・パネル */
--r-arch:68px;    /* カードメディアの上辺アーチ */
--bd:2.5px solid var(--ink);
--pop:5px 6px 0 var(--ink);      /* ハード影（ぼかさない） */
--pop-lg:9px 11px 0 var(--ink);  /* ホバー時 */
--ease:cubic-bezier(.2,.9,.25,1.15);
```

- **影は絶対にぼかさない。** `blur` を入れた瞬間に汎用SaaSの見た目に落ちる。
- ボタン・タグ・チップは `border-radius:999px` のピル。
- カードは内側に余白を持つ「台紙」構造。メディアは上辺だけ大きく丸めたアーチ。
- ホバーは `translate(-4px,-4px)` + 影を伸ばす。沈むのではなく浮かせる。

---

## 5. 図案（SVGモチーフ）— 2026-08-27 全面廃止

**線画のSVGアイコン／図案は、サイトでもSNS用の画像でも一切使わない。**

かつて `index.html` 冒頭に `<symbol>` スプライト29種を持ち、写真の無い商品カードの
代替表示に使う想定だった。2026-08-27に**スプライトごと削除**した。理由は2つ。

1. **安っぽく見える。** 64pxのグリッドに収めた線画アイコンを並べると、
   どれだけ配色を揃えてもフリー素材のピクトグラムに見える。
   このサイトが目指しているのは「カルチャー誌の物欲ボード」（10章）であって、
   汎用SaaSのフィーチャー一覧ではない。実際、Pinterestのピンで図案を3つ並べたところ、
   本人から「安っぽい」と指摘されて全面的に作り直した。
2. **1件も使われていなかった。** 全121商品に写真があり、`<use href="#m-...">` の
   参照は最終的にサイト内に1つも無かった。8.8KB / 185行が毎回配信されるだけの死にコードだった。

### 代わりに何を使うか

| やりたいこと | 使うもの |
|---|---|
| 面を持たせる・空きを埋める | カテゴリ色のベタ面、方眼、網点、紙のグレイン（4章・7章） |
| 商品を見せる | **写真**。商品カードは必ず写真を持たせる |
| SNS用の画像（ピン・OGP） | `public/images/` にあるサイト自前のムード写真・イラスト。<br>楽天の商品画像は使えない（親CLAUDE.md 5-3-3）ので、この自前素材が唯一の選択肢 |
| 見出しの強さ | **巨大な和文ディスプレイ（Dela Gothic One）**。図案ではなく文字で持たせる |

`PICKS` の各要素にはまだ `motif:"m-xxx"` というフィールドが残っているが、
**どこからも参照していない**。新規追加時に埋めても害はないが、意味は無い。

### この禁止に含まれないもの

- **UIの機能アイコン**（ハンバーガー、矢印、閉じる、SNSのロゴなど）は対象外。
  これは装飾ではなく操作の記号なので、必要なら使ってよい。
- **写真やイラストそのもの**（`public/images/` のムード画像は
  ベクターのイラストを含むが、これは「安っぽい線画アイコン」ではなく作品として成立している）

---

## 6. 動き

- ヒーロー = 読み込み時の段階表示（`animation-delay` で 0.04s 刻み）。**散らすより1回の演出にまとめた方が効く。**
- カード = `animation-timeline: view()` によるスクロール連動。`animation-fill-mode: backwards` にして範囲外では通常スタイルに戻し、ホバーの変形を殺さない。未対応ブラウザは IntersectionObserver にフォールバック（`main.js` の `sda` 判定）。
- ボタン = ポインタに吸い寄せる（マグネット）
- カード = 傾き。JSは `--rx` `--ry` を渡すだけにして、CSS側のホバー移動と合成させる
- 絞り込み = View Transition（`vtBusy` ガード必須。AGENTS.md 6章参照）
- ムードスライド = `data-mood-slider`。自動再生、ホバー停止、ドット操作。`prefers-reduced-motion` では自動再生しない

**すべて `@media (prefers-reduced-motion: reduce)` で止める。**

---

## 7. 禁止事項

- ❌ **ぼかした影**（`box-shadow` に blur を入れる）
- ❌ **紫〜青のグラデーション**、グラデーションを装飾目的で使うこと
- ❌ **Inter / Roboto / システムフォント**を見出しに使う
- ❌ カテゴリ色（`--c`）を文字色や文字の背景に使う（AA違反になる）
- ❌ 角丸カードを3列並べただけのヒーロー
- ❌ 意味の無い連番。商品カード番号は削除済みで、投稿日表示に置き換えている
- ❌ 全要素に散らしたアニメーション
- ❌ セール価格を `price` に書く（期間終了で嘘になる）
- ❌ 単色ベタ塗りの背景（紙のグレイン・方眼ドット・網点で質感を入れる）
- ❌ **線画のSVGアイコン／図案**（5章で全面廃止。装飾目的のピクトグラムは安っぽく見える。UIの機能アイコンは対象外）

---

## 8. レイアウト

- 最大幅 `--maxw:1160px`
- グリッドは3カラム（`grid-auto-flow:dense`）。860px以下で2カラム、560px以下で1カラム
- 写真ありのカードは2カラム占有のフィーチャー。誌面にリズムを出すための仕掛けで、`img` の有無で自動判定される
- 狭い画面では背景画像・色面・トップ戻りボタンが本文やCTAに重ならないか実測すること。旧 `.blob` 装飾は削除済み。
---

## 9. 2026-08-05 追記: デザインを豊かにする方向

既存の「ステッカー言語」は維持する。参考サイトから足すのは見た目の模写ではなく、次の原理だけ。

- ikiiki-being.com から: ものの背景にある気分や暮らしを、短い読みものとして見せる。単なる商品棚にしない。
- sou-group.jp から: 写真・イラスト・色面をセクションごとに使い分け、明るい勢いを出す。ただし色はカテゴリ識別の規律を崩さない。
- ヒーローには、人の気配があるラスターイラストを使ってよい。SVG図案だけで完結させず、手帖を作る手元・机・暮らしの小物など、サイト名とつながる場面を置く。
- 生成画像は必ず `public/images/` に保存し、WebPなど軽い形式に圧縮する。プロジェクト外の生成フォルダを直接参照しない。
- READセクションは「準備中」のガワを置かず、短くても独自コンテンツとして読める本文にする。アフィリエイトリンクなしの記事カードを優先する。
---

## 10. 2026-08-05 追記: ポップカルチャー寄りへの再定義

ユーザー指定により、「ていねいなくらし」やセピア寄りの手帖感から離し、ユニセックスなポップカルチャーのキュレーションサイトとして再構築する。

- 対象は10代から40代。ファッション、雑貨、インテリア、ガジェットを横断し、トレンド感と少しのエッジを出す。
- マガジンハウス系の雑誌をめくるような編集感を参考にする。ただし男性誌に寄せきらず、女性にも受け入れられる日常感を残す。
- セピア、淡いクラフト、丁寧な暮らし、ナチュラル一辺倒を避ける。紙色は明るく、赤・黄・ティール・紫・黒のコントラストで楽しい印象を作る。
- ヒーロー画像は「手元のスクラップ」より「カルチャー誌の物欲ボード」。スニーカー、バッグ、音楽、ガジェット、インテリアが同じ面にある状態が望ましい。
- 参照サイトからは、ビビッドな図形、強い丸み、写真とイラストの混在、セクションごとのリズムを取り入れる。見た目の模写はしない。
---

## 11. 2026-08-05 追記: UX再構築メモ

NiEWのようなカルチャーメディア感を、商品キュレーション向けに翻訳する。トップは単なる商品一覧の導入ではなく、編集部があるように見える体験にする。

- ヒーローは添付画像 `hero-pop-props.webp` を主役にする。メンズギア感や過密なコラージュより、ビビッドな日用品・雑貨・インテリアのプロップスタイリングを優先。
- ヒーロー直下にムードボードとタグ導線を置き、カテゴリだけでなく「色」「部屋」「持ち歩き」「読みもの」から回遊できるようにする。
- モーションは、スクロール時の立ち上がり、ホバーの浮き、ムード面の軽い追従に限定する。過剰な3Dや意味のない浮遊は避ける。
- 情報密度は上げるが、商品リンクやアフィリエイトURLには触れない。編集表現と購入導線を分ける。
---

## 12. 2026-08-05 追記: トーン調整

ポップに振り切りすぎたため、「日常に馴染む、デザインで生活を少し上げる」方向へ戻す。定番からエッジ、時にラグジュアリーまで扱うアイテムキュレーションとして見せる。

- `POP CULTURE CURATION` の言い方は避け、アイテムのキュレーションとして表現する。
- ブランドスクロール帯は不要。商品発見の邪魔になるため削除。
- カード番号は不要。投稿日を表示し、新着/更新感を出す。
- 商品カードは初期6件まで。残りは「もっと見る」で展開する。
- 写真なしカードのSVGモチーフは主役にしない。チープに見える場合は、カテゴリ名と抽象的な色面で編集的に見せる。
- 見出しはムードタイルのような丸く太い日本語フォント寄りにそろえる。

---

## 13. 2026-08-05 追記: 装飾とロゴの再調整

丸いSVG風の装飾や全面ドットはチープに見えやすいため、主役にしない。参照サイトのように、背景そのものを大きな色面・罫線・チェック/ストライプで構成し、雑誌の誌面に近い編集感を作る。

- ヒーロー右上の丸装飾は使わない。写真の背後には斜めの大きな色面や紙片を置く。
- ロゴは小さな丸マークではなく、文字組みでブランド感を出す。日本語名を太めに、英字サブを小さく添える。
- ドット背景は原則避ける。使う場合も全面ではなく、意味のある範囲に限定する。
- 画像なしカードはSVGモチーフに戻さない。カテゴリ名、紙片、ストライプで編集的に処理する。
---

## 14. 2026-08-05 追記: トップ再構築とムードスライド

トップは商品写真をカード内に閉じ込めず、`hero-pop-props.webp` を背景として大きく見せる。白い余白面と画像の色を重ねて、雑誌の表紙に近い第一印象を作る。

- ヒーロー画像は画面全体の背景として使い、コピーは左側の読みやすい余白に置く。
- `MOOD BOARD` / `EDITOR'S FILTER` / 選び方説明は固定カードではなく、スライドで順に見せる。
- スライドは自動再生するが、ホバーで停止し、`prefers-reduced-motion` では自動再生しない。
- ムード4枠は生成写真を使う。暗い写真や地味な写真は避け、ピンク・イエロー・グリーンを自然に含む明るいプロップ写真を優先する。
- EDITOR'S FILTERのイラストは淡い手描きではなく、サイトのポップさに合う現代的な編集イラストにする。
---

## 15. 2026-08-06 追記: 最新トップ/スライド状態

2026-08-06時点の最新ローカル確認は `http://127.0.0.1:5178/?verify=21-final`。`index.html` のキャッシュ番号は `style.css?v=21` / `main.js?v=21`。

- ヒーローは `hero-pop-props.webp` を全面背景にし、左側にコピーを置く。黒い仕切り線は廃止し、下端は紙面が切り替わる波形エッジにしている。
- ムードスライドは `MOOD BOARD` / `EDITOR'S FILTER` / `HOW TO PICK` の3枚。各スライドのイラストは画像カードではなく、スライドフレーム全体の背景として敷く。
- スライド背景画像は `slide-mood-board.webp` / `slide-editors-filter.webp` / `slide-how-to-pick.webp`。ポップアート寄りのフラット線画、左側にテキスト余白を持つ横長構図。
- スライド高さは PC `320px`、860px以下 `390px`、560px以下 `430px`。ユーザーは「上部の余白っぽさ」に敏感なので、増やす場合は理由を持つこと。
- 「もっと見る」は `.more-btn` の専用デザイン。初期表示は6件、展開時は「閉じる」。フィルタ変更時は閉じた状態に戻す。
- 右下に `.back-top` 固定ボタンを追加。スクロール520px超で表示し、クリックでページ上部へ戻る。
- `editor-illustration.webp` は以前の単独EDITOR'S FILTER用画像。現在のHTMLからは参照していない。不要なら削除候補だが、判断前に参照検索すること。